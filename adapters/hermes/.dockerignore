# Don't bloat the image with operator docs / scripts that have no
# runtime role. Only entrypoint.sh, the Dockerfile, and the runtime
# files under sidecar/ (sidecar.py + requirements.txt) need to reach
# the image build context.
README.md
smoke.sh
sidecar/README.md
sidecar/smoke.sh
